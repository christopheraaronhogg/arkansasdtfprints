Showing Order Status on Admin Page
Reasoned for 1m 3s
Below is an updated snippet of your admin page that explicitly displays the order status in a badge—including “Processing”—and allows you to filter by “Pending,” “Processing,” or “Completed.” This way, you’ll see the status on each row and also have the option to filter by all three states.

1) Updated HTML Markup
html
Copy
{% extends "base.html" %}

{% block content %}
<div class="container">
    <header class="d-flex align-items-center justify-content-between">
        <div>
            <h1 class="page-title"><i class="fas fa-clipboard-list me-2"></i>DTF Print Orders</h1>
            <p class="subtitle mb-0">View and manage your print orders</p>
        </div>
        <div class="d-flex gap-2">
            <button class="upload-btn" onclick="toggleFilterModal()">
                <i class="fas fa-filter me-1"></i>Filter
            </button>
            <button class="upload-btn">
                <i class="fas fa-download me-1"></i>Export
            </button>
        </div>
    </header>

    <div class="order-list mt-4">
        {% for order in orders %}
        <div class="order-item" 
             data-status="{{ order.status }}"
             data-date="{{ order.created_at.strftime('%Y-%m-%d') }}">
            <!-- Left: Order Info -->
            <div class="order-details">
                <h5 class="order-number mb-1">{{ order.order_number }}</h5>
                <div class="order-meta text-muted">
                    {{ order.created_at.strftime('%B %d, %Y %I:%M %p') }} <br>
                    {{ order.email }}
                </div>
            </div>

            <!-- Middle: Image Previews -->
            <div class="order-images">
                {% for item in order.items %}
                <div class="img-container">
                    <img src="{{ url_for('get_order_image', order_id=order.id, filename=item.file_key) }}"
                         alt="Preview of {{ item.file_key }}"
                         onclick="showFullSizePreview(this.src)">
                </div>
                {% endfor %}
            </div>

            <!-- Right: Status, Cost, and Button -->
            <div class="order-actions">
                <!-- Show status as a badge, with different colors for each status -->
                <span class="badge bg-{% if order.status == 'completed' %}
                    success
                {% elif order.status == 'pending' %}
                    warning
                {% elif order.status == 'processing' %}
                    secondary
                {% else %}
                    info
                {% endif %}">
                    {{ order.status|title }}
                </span>
                <div class="order-cost">
                    ${{ "%.2f"|format(order.total_cost) }}
                </div>
                <a href="{{ url_for('view_order', order_id=order.id) }}" class="details-btn">
                    <i class="fas fa-eye me-1"></i>Details
                </a>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<!-- Filter Modal -->
<div id="filterModal" class="modal filter-modal" style="display: none;">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Filter Orders</h3>
            <span class="close-modal" onclick="toggleFilterModal()">&times;</span>
        </div>
        <div class="modal-body">
            <div class="mb-3">
                <label class="form-label">Status</label>
                <select id="statusFilter" class="form-select">
                    <option value="">All</option>
                    <option value="pending">Pending</option>
                    <option value="processing">Processing</option>
                    <option value="completed">Completed</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label">Date Range</label>
                <div class="d-flex gap-2">
                    <input type="date" id="startDate" class="form-control">
                    <input type="date" id="endDate" class="form-control">
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button class="upload-btn" onclick="applyFilters()">Apply Filters</button>
            <button class="btn btn-secondary" onclick="clearFilters()">Clear</button>
        </div>
    </div>
</div>

<!-- Image Preview Modal -->
<div id="previewModal" class="modal preview-modal">
    <span class="close-modal" onclick="closePreviewModal()">&times;</span>
    <img id="modalImage" class="modal-image" src="" alt="Full size preview">
</div>

<script>
function showFullSizePreview(src) {
    const modal = document.getElementById('previewModal');
    const modalImg = document.getElementById('modalImage');
    modal.style.display = 'flex';
    modalImg.src = src;
}

function closePreviewModal() {
    document.getElementById('previewModal').style.display = 'none';
}

function toggleFilterModal() {
    const modal = document.getElementById('filterModal');
    modal.style.display = (modal.style.display === 'none') ? 'flex' : 'none';
}

function applyFilters() {
    const status = document.getElementById('statusFilter').value;
    const startDate = document.getElementById('startDate').value;
    const endDate = document.getElementById('endDate').value;

    const orders = document.querySelectorAll('.order-item');

    orders.forEach(order => {
        let show = true;

        // Filter by status
        if (status && order.dataset.status !== status) {
            show = false;
        }

        // Filter by date range
        if (startDate || endDate) {
            const orderDate = order.dataset.date;
            if (startDate && orderDate < startDate) show = false;
            if (endDate && orderDate > endDate) show = false;
        }

        order.style.display = show ? 'flex' : 'none';
    });

    toggleFilterModal();
}

function clearFilters() {
    document.getElementById('statusFilter').value = '';
    document.getElementById('startDate').value = '';
    document.getElementById('endDate').value = '';

    const orders = document.querySelectorAll('.order-item');
    orders.forEach(order => order.style.display = 'flex');

    toggleFilterModal();
}

// Close modals when clicking outside
document.addEventListener('click', function(event) {
    const filterModal = document.getElementById('filterModal');
    const previewModal = document.getElementById('previewModal');

    if (event.target === filterModal) {
        filterModal.style.display = 'none';
    }
    if (event.target === previewModal) {
        previewModal.style.display = 'none';
    }
});
</script>
{% endblock %}
Changes to Note
<span class="badge bg-...">

Now supports three statuses: pending, processing, and completed.
Uses Bootstrap contextual classes: bg-warning, bg-secondary, bg-success, etc.
Filter Dropdown

Added Processing to the <select>:
html
Copy
<option value="processing">Processing</option>
Badge Colors

For demonstration:
Pending → bg-warning (yellow)
Processing → bg-secondary (gray)
Completed → bg-success (green)
Anything else → bg-info (blue)
This ensures you see a clear, color-coded status for each order on the admin page, and you can filter by any of those three statuses if desired. Enjoy!